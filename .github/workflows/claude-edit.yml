name: Claude Edit

on:
  workflow_dispatch:
    inputs:
      prompt:
        description: Edit request prompt
        required: true
        type: string
      job_id:
        description: Job UUID for webapp callback (optional)
        required: false
        type: string
        default: ''

concurrency:
  group: claude-edit
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  edit:
    runs-on: [self-hosted, macOS]
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Configure git
        run: |
          git config user.name "devprofile-admin-bot"
          git config user.email "bot@devprofile-admin.local"

      - name: Write prompt file
        env:
          PROMPT: ${{ inputs.prompt }}
        run: |
          mkdir -p /tmp/claude-edit
          printf '%s' "$PROMPT" > /tmp/claude-edit/prompt.txt

      - name: Run Claude Code
        run: bash scripts/run-claude.sh /tmp/claude-edit/prompt.txt

      - name: Commit and push
        id: commit
        env:
          JOB_ID: ${{ inputs.job_id }}
        run: |
          git add -A
          if git diff --cached --quiet; then
            echo "No changes produced"
            echo "commit_sha=" >> "$GITHUB_OUTPUT"
            echo "new_pages=" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          MSG="chore: apply claude edit [job:${JOB_ID:-manual}]"
          git commit -m "$MSG"
          git push origin HEAD:main
          SHA=$(git rev-parse HEAD)
          NEW_FILES=$(git diff HEAD~1 HEAD --name-only --diff-filter=A | grep '^projects/' | head -5 | paste -sd ',' -)
          echo "commit_sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "new_pages=$NEW_FILES" >> "$GITHUB_OUTPUT"

      - name: Notify webapp
        if: always() && inputs.job_id != ''
        env:
          JOB_ID: ${{ inputs.job_id }}
          COMMIT_SHA: ${{ steps.commit.outputs.commit_sha }}
          NEW_PAGES: ${{ steps.commit.outputs.new_pages }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          STATUS_FINAL: ${{ job.status }}
          CALLBACK_SECRET: ${{ secrets.CALLBACK_SECRET }}
        run: |
          if [ -z "$CALLBACK_SECRET" ]; then
            echo "CALLBACK_SECRET not set — skipping notify"
            exit 0
          fi
          STATUS="success"
          if [ "$STATUS_FINAL" != "success" ]; then STATUS="failed"; fi
          curl -sS -X POST https://devprofile-admin.vercel.app/api/callback \
            -H "Authorization: Bearer $CALLBACK_SECRET" \
            -H "Content-Type: application/json" \
            --data-binary @- <<EOF || echo "callback failed (non-fatal)"
          {
            "job_id": "$JOB_ID",
            "status": "$STATUS",
            "commit_sha": "$COMMIT_SHA",
            "new_pages": "$NEW_PAGES",
            "workflow_url": "$RUN_URL"
          }
          EOF

      - name: Cleanup
        if: always()
        run: rm -rf /tmp/claude-edit
